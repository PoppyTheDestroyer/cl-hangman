'use strict';

var _uniqueRandomArray = require('unique-random-array');

var _uniqueRandomArray2 = _interopRequireDefault(_uniqueRandomArray);

var _simpsonsNames = require('./simpsons-names.json');

var _simpsonsNames2 = _interopRequireDefault(_simpsonsNames);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var getRandomItem = (0, _uniqueRandomArray2.default)(_simpsonsNames2.default);

module.exports = {
  all: _simpsonsNames2.default,
  random: random
};

function random(number) {
  if (number === undefined) {
    return getRandomItem();
  }

  var randomItems = [];
  for (var i = 0; i < number; i++) {
    randomItems.push(getRandomItem());
  }
  return randomItems;
}

//# sourceMappingURL=index.js.map